# 평가와 검증 범위

`test_records.py`는 기록 무결성 검사를 검증한다. 외부 서비스·원본 스킬·추가 패키지가 필요 없다.

```bash
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s /absolute/package/evals -p 'test_*.py' -v
```

`fixture.py`는 가상 문서와 예시 레코드를 만든다. 수치·제품·검토 기록은 전부 시험용이며 실제 연구나 독립 검토 결과가 아니다. 실제 연구처럼 위장하여 전달하지 않는다.

## 행동 평가

행동 평가에는 패키지, 사용자 요청, 원자료만 제공한다. 의도한 답이나 완성된 장부·보고서는 전달하지 않는다. 현재 호스트에서 별도 평가 역할이 허용되면 작성 맥락과 분리해 검토할 수 있다. 같은 작성자가 수행한 점검은 자기점검으로 표시한다.

평가 전 요청·원자료·성공 기준을 고정하고, 결과에 맞추어 기준을 바꾸지 않는다. 평가 기록은 실행 환경·사용한 도구·실제 열람·시간·오류·확인한 범위를 보존한다.

| 평가 차원 | 판단 기준 |
|---|---|
| 폭 | 중요한 대상·기간·관점·대안이 누락되지 않았으며 빈 축을 보강했는가 |
| 깊이 | 원문 방법·조건·인용 계보·후속 검증·반대 증거를 실제로 추적했는가 |
| 정확성 | 핵심 문장이 올바른 원자료와 연결되고 상충·조건부·미확정을 적절하게 표현했는가 |
| 추적성 | 질문부터 결론·필요한 요건·실행까지 의미 있는 연결이 있는가 |
| 검토 | 강한 해석과 합리적인 방어 논리를 점검하며 결함을 억지로 만들지 않았는가 |
| 유용성 | 독자가 무엇을 어떤 조건에서 판단하고 다음에 무엇을 확인할지 이해할 수 있는가 |
| 자원 | 실제 사용량을 측정하고 조사 품질 대비 추가 단계의 효과를 설명하는가 |

구조 검사가 통과해도 의미상 허위 인용이나 조작된 검토 기록을 탐지한다고 주장하지 않는다. 보고서의 새로운 핵심 단정 누락 여부와 실제 수행은 별도 자료 읽기·실행 기록으로 확인한다. 정답률·독립 검토 성능·상용 주제 전반의 우수성은 실제 평가 데이터 없이 수치로 제시하지 않는다.

검사 실패를 유도하는 사례는 초기화 덮어쓰기, 원문 요청 변경, 근거 파일 변경, 최종 보고서 수정, 읽지 못한 출처 사용, 수집 관점·깊이 누락, 열린 중요 리드, 과장된 완료, 미확정 단정, 유령 출처, 검토 범위 누락, 순환 추적, 외부 경로·심볼릭 링크, 과거 검토 이력, 재인용 그룹 위장이다.

1.1.0의 문헌 검색·실증 방법은 `cross-corpus-versions`, `retrieval-gap-without-cap`, `experimental-comparison-contract`, `experimental-negative-vs-error`, `experimental-lineage` 사례로 별도 행동 점검한다. 이 사례들은 평가 지침이며 기본 자동 회귀시험에 포함된다는 뜻은 아니다. 실제 수행한 결과와 미수행 범위를 구분해 기록한다.

## 검색 보완 회귀시험

`test_retrieval_integrity.py`는 읽기 가능한 반박/원자료 요건, 근거 노트 전용 검색, 바이너리 누락 공개, 전체 토큰 창 커버리지, 뒷부분 창 점수, 창 변조 탐지, 구벡터 재계산을 검사한다. CharacterTokenizer와 벡터는 가상 시험 장치이며 실제 언어 검색 성능 자료가 아니다. `check_tokenizer.py`는 별도 환경의 실제 고정 리비전 토크나이저로 잘림 없는 창과 전체 입력 범위를 검사하며, 기본적으로 다운로드하지 않는다. 실행하지 않은 모델 검색 순위·에이전트 행동 평가는 미수행으로 남긴다.
