---
name: my-dear-research-center
description: "나의 친애하는 연구센터. ISP/ISMP 흐름으로 자료를 폭넓고 깊게 수집하고, 원출처 추적·교차검증·반증·적대적 검토를 거쳐 근거 있는 결론과 필요한 실행계획을 만든다. 연구센터를 지목하거나 심층 조사, 다중 출처 비교, 전략 연구, ISP/ISMP 기획을 요청할 때 사용한다. 단순 사실 확인·문장 수정·일반 코드 수정에는 전체 연구 흐름을 적용하지 않는다."
metadata:
  version: "1.4.1"
---

# 나의 친애하는 연구센터

질문에 필요한 자료를 **폭넓고 깊게** 조사하고, 근거가 어떤 판단과 실행 요건으로 이어지는지 설명한다. 기본 조사 강도는 `broad-deep`다. 짧은 최종 답변을 요청받아도 수집·검증을 자동으로 축소하지 않는다. 사용자가 범위·기한·자원을 제한하면 그 조건을 기록하고 조사 결과의 한계로 반영한다.

연구·검토 방법론은 이 패키지에 내장되어 있다. 다른 리서치 스킬, 원본 적대자 스킬, 그 CLI·상태 파일을 실행 중 참조하지 않는다. 호스트의 검색·브라우저·파일·계산 도구를 직접 사용한다. 내부 파일의 상대 경로는 이 `SKILL.md`가 있는 폴더 기준이다.

## 시작과 범위

1. 사용자 자료와 현재 작업을 먼저 확인한다. 원문 요청을 보존하고 후속 지시·가정·범위·독자·기준 시점·산출물·성공 기준을 별도로 적는다. 중단된 연구라면 [재개 절차](references/delivery-and-resume.md)를 따른다.
   관련 과거 연구가 있으면 [전역 색인](references/global-index.md)의 `runs/check`로 대상과 현재성을 확인한다. 시작·재개·전역 검색 전에 선택한 실행에 `refresh --run <실행> --reason start|resume|before_search --semantic off`를 호출해 필요한 변경을 반영한다. 의미 검색을 사용할 때는 `--semantic required`로 갱신하고 미구성·미반영 범위를 알린다. 과거 결과를 현재 사실로 재사용하지 않고 원본 실행·시점·근거를 다시 확인한다.
2. 질문을 검증 가능한 하위 질문으로 분해한다. 명시된 대상·지역·기간·비교 기준을 빠짐없이 연결하고 결론에 중요한 질문을 `key`로 표시한다. 중요한 미정 사항만 질문하며 추론 가능한 사항은 기록하고 진행한다.
3. 연구 디렉터리는 사용자가 지정한 곳 또는 현재 작업 공간의 `work/research/<주제-시각>/`를 사용한다. [기록 계약](references/evidence-contract.md)에 따라 아래 도구로 초기화한다. 기존 디렉터리는 덮어쓰지 않는다.

```bash
python3 /absolute/path/to/my-dear-research-center/scripts/research.py init /absolute/run --query-file /absolute/query.md --purpose decision
```

`purpose`는 `explanation`, `decision`, `implementation` 중 선택한다. 이 선택은 필요한 결과 형식을 정하며 기본 수집 강도를 낮추지 않는다. Python 3.10 이상과 표준 라이브러리만 사용한다. Python을 사용할 수 없으면 같은 자료 형식을 수작업으로 유지하고 자동 검사가 미수행임을 명시한다.

초기화할 때 `.research-index.json`에 경로와 독립적인 실행 ID가 생긴다. 전역 보존을 제한한 연구는 `init --index-scope local|off`로 시작한다. 기존 연구의 제외 지시에는 `index.py scope --run <실행> --set local|off`를 적용한다. 사용자가 지정한 보존 범위를 따른다.

## 연구 흐름

학술 검색·공개 원문 확보·PDF 추출·기술 문서 연결·수집 자료 재검색에는 [수집 도구와 실행 환경](references/collection-tools.md)을 적용한다. `scripts/collect.py`의 실제 검색 결과·추출 페이지·SQLite 기록을 기존 근거 장부에 연결하되, 자동 수집을 정독·주장 검증으로 취급하지 않는다. 개인 이메일·인터프리터 설정은 배포 패키지 밖에 보관한다.

여러 연구 실행의 지식을 누적하거나 과거 근거를 다시 찾을 때 [전역 연구 색인](references/global-index.md)을 적용한다. AI가 현재 작업·이미 등록된 실행에서 범위를 선택하고, `scripts/index.py refresh`가 검증·증분 동기화·벡터 재사용을 수행한다. 자료 묶음의 저장·추출·장부 반영을 마친 지점에서 `--reason collection_complete --semantic off`로 갱신한다. 전체 홈 폴더 탐색이나 상시 실행 서비스는 필요하지 않다. 색인은 파생 자료이며 원본 연구 폴더와 근거 장부를 대체하지 않는다. 의미 유사도와 그래프 연결만으로 주장을 지지하거나 독립 근거로 세지 않는다.

검색 응답의 `coverage`를 확인한다. `stale`, `missing`, `busy`, `identity_conflict`, 오류 상태의 자료는 기본 검색에서 빠지므로 검색 결과 없음과 자료 없음은 다르다. 관련 실행 중 갱신 가능한 대상을 반영하고 다시 검색한다. 과거 저장본이 필요한 경우에만 `--allow-stale`로 조회하며 그 시점을 명시한다. 제외 지시는 과거 저장본 검색에도 적용한다.

새 연구에서는 [자료 수집 방법](references/research-methods.md)과 [증거 계약](references/evidence-contract.md)을 읽는다. 전략·대안·요건을 구성할 때 [ISP/ISMP 흐름](references/isp-ismp-flow.md), 초안 전과 최종 검토에 [내장 적대적 검증](references/adversarial-review.md), 전달·재개에 [전달 규칙](references/delivery-and-resume.md)을 읽는다. 출처 계보 문서는 유지보수 때만 필요하다.

실험·재현·실측으로 판단할 질문에는 [실증 연구 방법](references/experimental-research.md)을 추가로 적용한다. 문헌 수집의 폭과 깊이는 그대로 유지하며, 실험 절차는 해당 질문에 필요한 경우에만 사용한다.

| 단계 | 해야 할 일 | 다음 단계에 필요한 결과 |
|---|---|---|
| 연구 정의 | 원문과 하위 질문, 범위·조건·성공 기준 연결 | 질문과 조사 범위 |
| 환경 탐색 | 분야·대상·시점·관점별 탐색, 원자료·반대 근거 발견 | 질문별 수집 범위와 출처 후보 |
| 현황·차이 진단 | 원문 정독, 인용 계보·후속 연구 추적, 실증·모순·누락 분석 | 주장 장부와 중요 쟁점 |
| 방향·평가 기준 | 근거에 따라 문제 우선순위·목표·선택 기준 정의 | 판단 기준과 가정 |
| 대안·목표안 | 가능한 대안과 현행 유지안 비교, 성립 조건·민감도 확인 | 근거 있는 결론·권고 |
| 상세 요건 | 선택한 안의 기능·데이터·품질·운영·검증 조건 상세화 | 추적 가능한 요건과 수용 기준 |
| 이행·전달 | 선후관계·역할·자원 근거·성공 및 중단 조건 정리 | 요청한 결과와 중요한 한계 |

학술·비교·설명 요청에서는 현황을 현재 지식과 논쟁으로 해석한다. 실행 요건이 필요하지 않으면 마지막 단계들을 결론의 적용 조건과 후속 검증으로 축약한다. 정식 ISP/ISMP 산출물은 적용 가이드와 발주 범위를 확인하여 확장한다.

## 폭과 깊이를 확보하는 규칙

- **폭:** 모든 중요 질문을 기본 사실·원자료·반대 근거·적용 조건의 네 관점으로 조사한다. 분야에 맞는 언어·지역·시점·자료 종류·이해관계자를 포함한다. 이미 풍부한 축보다 비어 있는 축을 먼저 보강한다.
- **검색 조합:** 정확한 용어·식별자의 검색과 질문 의미에 따른 탐색을 조합하고, 관련 학술 자료원의 후보를 함께 평가한다. 실제 의미 검색 기능이 없으면 대체 검색을 명시한다. 같은 논문의 여러 색인 노출은 중복 처리하되 결과가 달라진 판본은 보존한다.
- **깊이:** 중요한 주장의 원문에서 방법·표·조건·한계를 읽고, 재인용은 원발행 자료까지 추적한다. 인용된 기초 자료, 후속 검증·수정·철회, 실패 사례, 경쟁 설명을 따라간다. 요약문끼리 교차 인용하는 것으로 끝내지 않는다.
- **확장:** 새 자료가 제시한 미조사 쟁점을 `leads.jsonl`에 남기고 중요도에 따라 조사한다. 중복·무관·접근 불가·유보 처리도 이유를 남긴다. 중요한 열린 쟁점을 가린 채 종료하지 않는다.
- **재검토:** 초안 전에 ‘무엇이 발견되면 현재 결론이 바뀌는가’를 물어 증거 공백을 보강한다. 실제 모순과 기간·정의·대상 차이를 구분한다. 반박 검색의 빈 결과를 사실 확정으로 취급하지 않는다.
- **충분성:** 출처·검색·반복의 고정 개수로 합격하거나 중단하지 않는다. 핵심 질문의 근거와 중요한 반론·조건을 확인했는지, 추가 조사로 판단이 바뀔 쟁점이 남았는지 판단한다. 자원 제한으로 중단하면 그 한계와 미조사 목록을 전달한다.

## 근거와 검토

`질문 → 출처의 해당 근거 → 주장 → 문제·대안·판단 → 필요한 요건·실행`을 연결한다. 관찰, 출처의 주장, 연구센터의 추론을 구분한다. 주장은 `supported`, `conditional`, `unresolved`, `refuted`로 기록하며 판단 이유와 적용 조건을 붙인다.

원문이 해당 주장을 실제로 지지하는지 읽어서 확인한다. 인용 개수, 도메인 수, 파일 존재나 검사 성공은 사실의 진실성을 증명하지 않는다. 전재·동일 데이터 재분석은 공통 원천을 기록하고 독립 근거로 중복 계산하지 않는다. 원문 내용을 확인하지 못했으면 확인한 범위 안에서만 사용한다.

초안 전 증거 검토와 최종 핵심 주장 검토를 수행한다. 검토는 가장 강한 해석, 숨은 전제, 합리적인 방어 논리, 파생 결론까지 다룬다. 결함을 억지로 만들거나 불확실성을 확정적인 반박으로 바꾸지 않는다. 실제 결함을 수정한 뒤 영향받은 결과를 다시 검토한다.

실측 비교는 가설·기준선·평가 기준·변경 변수를 먼저 기록한다. 실제 결과를 얻은 실험의 코드·조건·로그를 보존하고 새 가설은 별도 후속 기록으로 연결한다. 실행 실패와 가설을 반박한 결과를 구분하며, 성공 상태만 보고 성능·재현성을 단정하지 않는다.

연구의 기록·종합 책임자는 한 명이다. 위임이 허용되고 유용할 때만 독립된 조사나 검토 역할을 배정하고 질문·범위·입출력·종료 조건을 준다. 외부 공급자나 모델을 자동으로 선택하지 않는다. 같은 맥락의 점검을 독립 검토라고 쓰지 않는다.

## 마감

1. 보고서의 핵심 단정과 장부를 대조하고 `report-map.jsonl`에 해당 문장과 주장 ID를 연결한다. 불확실성은 관련 결론 가까이에 둔다. 요구사항·행동에는 근거와 수용 기준이 있어야 한다.
2. [최종 검토 기록과 입력 지문](references/evidence-contract.md)을 만들고 실제 검토 결과를 기록한다. 검토 뒤 보고서·근거가 바뀌면 영향 범위를 다시 확인한다.
3. 실행 디렉터리를 검사한다.

```bash
python3 /absolute/path/to/my-dear-research-center/scripts/research.py validate /absolute/run --final
```

4. 전역 보존이 허용된 연구는 `index.py refresh --run <실행> --reason closeout --semantic auto`로 갱신한다. 이미 설정된 의미 환경은 누락 벡터를 처리하며, 새 모델을 자동으로 내려받지 않는다. 실패·중단·미구성 상태를 숨기지 않고 다음 재개 때 필요한 처리를 이어간다. 연구 검토 완료와 색인 갱신 상태를 따로 확인한다. 정상 색인의 내부 로그는 길게 전달하지 않아도 된다.

검사는 기록 구조·참조·변경 여부만 검사한다. 실패를 수정하고 다시 검사하되, 통과를 만들기 위해 범위나 증거 수준을 낮추지 않는다. `records_valid`는 사실 검증이나 독립 검토의 성능 보증이 아니다.

사용자가 요청한 형식으로 결론과 중요한 근거·한계·필요한 다음 행동을 전달한다. 아티팩트를 요청하지 않은 경우 채팅으로 전달할 수 있다. 내부 기록이나 여러 출력 형식을 자동으로 모두 전달하지 않는다.
